# Required for Firebase App Check on web builds.
# Create a reCAPTCHA v3 site key at https://www.google.com/recaptcha/admin
# and register it in Firebase Console → Project Settings → App Check.
EXPO_PUBLIC_RECAPTCHA_SITE_KEY=
